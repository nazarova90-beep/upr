"""Настройка логирования.

На текущем шаге — заглушка. Реальная конфигурация (структурированные
логи в JSON, корреляционный id запроса и т.д.) добавится в hello-world
и далее.
"""


def setup_logging() -> None:
    """Инициализация логгеров приложения.

    Сейчас не делает ничего — логирование пойдёт через стандартный
    `logging` с дефолтными настройками. Заполнение — следующая задача.
    """
    return None
