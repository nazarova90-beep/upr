"""API-схемы домена «Тренировка» (вход/выход HTTP).

Внимание: это **API-схемы** (request/response модели Pydantic).
Сами таблицы БД лежат в `app/db/models/workout.py`. На MVP с SQLModel
обе роли может играть один класс, но в скелете оставляем разделение,
чтобы было место для специфичных API-форм (например, Public-варианты
без чувствительных полей).

На текущем шаге — пусто.
"""
