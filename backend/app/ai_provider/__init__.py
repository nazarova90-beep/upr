"""Абстракция AI-провайдера.

Любая работа с языковой моделью идёт через интерфейс `AIProvider` из
`base.py`. Конкретные реализации (Gemini сейчас, GPT/Claude в будущем)
лежат рядом отдельными файлами. Это позволяет менять провайдера без
правок бизнес-логики (см. BACKEND.md, принцип «замена внешних звеньев
без переписывания»).
"""
