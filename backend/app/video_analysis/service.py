"""Бизнес-логика домена «Анализ видео».

Принимает файл, делегирует сохранение в `app/storage/`, передаёт в
`app/ai_provider/` для анализа, сохраняет результат через
`app/db/models/video_analysis.py`. На текущем шаге пуст.
"""
