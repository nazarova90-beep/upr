"""Домен «Анализ видео».

Принимает загруженные клиентом видео упражнений, складывает в
хранилище (через `app/storage/`), отправляет в AI-провайдер (через
`app/ai_provider/`) и сохраняет результаты анализа. На текущем шаге —
структурный каркас.
"""
