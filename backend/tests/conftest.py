"""Общие фикстуры pytest для всех тестов backend.

На текущем шаге пусто. Здесь со временем появятся:
- фикстура `client` с FastAPI TestClient,
- фикстура `session` с временной in-memory SQLite БД,
- общие моки AI-провайдера.
"""
