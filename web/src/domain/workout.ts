/**
 * Доменный тип Workout (тренировка) и связанная сущность VideoAnalysis.
 *
 * Это TS-зеркало серверных моделей `backend/app/db/models/workout.py`
 * и `backend/app/db/models/video_analysis.py`. Поля появятся в Phase 2.
 */
export type Workout = Record<string, never>;
export type VideoAnalysis = Record<string, never>;
